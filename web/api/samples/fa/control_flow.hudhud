// فارسی - جریان کنترل
متغیر شمارنده = 0;
متغیر حداکثر = 5;

// Loop
تا_وقتی (شمارنده < حداکثر) {
    شمارنده = شمارنده + 1;
}

// Conditional
متغیر امتیاز = 85;

اگر (امتیاز >= 90) {
    امتیاز;
} وگرنه {
    اگر (امتیاز >= 80) {
        امتیاز;
    } وگرنه {
        امتیاز;
    }
}

شمارنده;
