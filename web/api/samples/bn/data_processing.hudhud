// বাংলা - ডেটা প্রক্রিয়াকরণ
// বাংলা - নেটিভ কীওয়ার্ড সহ ডেটা প্রক্রিয়াকরণ

// ডেটা সংজ্ঞায়িত করুন
ধরো ডেটা১ = ১০০;
ধরো ডেটা২ = ২০০;
ধরো ডেটা৩ = ৩০০;

// Calculate
ধরো মোট = ডেটা১ + ডেটা২ + ডেটা৩;
ধরো গড় = মোট / ৩;

গড়;