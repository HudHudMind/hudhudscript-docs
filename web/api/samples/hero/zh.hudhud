// 中文 - Demo
函数 问候(名字) {
    返回 "你好，" + 名字 + "!";
}

函数 加(甲, 乙) {
    返回 甲 + 乙;
}

变量 消息 = 问候("世界");
打印(消息);

变量 总和 = 0;
变量 计数 = 1;
当 (计数 <= 10) {
    总和 = 加(总和, 计数);
    计数 = 计数 + 1;
}
打印("总和 1-10: " + 总和);
