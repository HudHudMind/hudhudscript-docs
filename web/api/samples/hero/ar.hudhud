// العربية - Demo
دالة رحب(اسم) {
    ارجع "مرحباً، " + اسم + "!";
}

دالة اجمع(أ, ب) {
    ارجع أ + ب;
}

متغير رسالة = رحب("العالم");
اطبع(رسالة);

متغير مجموع = ٠;
متغير عداد = ١;
بينما (عداد <= ١٠) {
    مجموع = اجمع(مجموع, عداد);
    عداد = عداد + ١;
}
اطبع("المجموع ١-١٠: " + مجموع);
