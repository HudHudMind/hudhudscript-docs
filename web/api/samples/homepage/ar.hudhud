// العربية - دالة الترحيب

دالة رحب(اسم) {
    متغير رسالة = "مرحباً, " + اسم + "!";
    اطبع(رسالة);
    ارجع رسالة;
}

دالة اجمع(أ, ب) {
    ارجع أ + ب;
}

متغير نتيجة = رحب("العالم");
اطبع(نتيجة);

متغير مجموع = اجمع(٤٢, ٥٨);
اطبع("مجموع: " + مجموع);
