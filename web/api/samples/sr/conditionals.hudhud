// Српски - Условна логика
нека резултат = 85;

ако (резултат >= 90) {
    ispiši(1000);
} иначе {
    ако (резултат >= 80) {
        ispiši(2000);
    } иначе {
        ispiši(3000);
    }
}

ispiši(999);
