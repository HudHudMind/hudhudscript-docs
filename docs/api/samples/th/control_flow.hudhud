// ไทย - การควบคุมการไหล
ตัวแปร ตัวนับ = 0;
ตัวแปร สูงสุด = 5;

// Loop
ขณะที่ (ตัวนับ < สูงสุด) {
    ตัวนับ = ตัวนับ + 1;
}

// Conditional
ตัวแปร คะแนน = 85;

ถ้า (คะแนน >= 90) {
    คะแนน;
} มิฉะนั้น {
    ถ้า (คะแนน >= 80) {
        คะแนน;
    } มิฉะนั้น {
        คะแนน;
    }
}

ตัวนับ;
