// Српски - Низови
// Низови са домаћим кључним речима

// Креирај низове
нека бројеви = [1, 2, 3, 4, 5];
нека имена = ["Ана", "Иван", "Маја"];

// Приступи елементима
нека први = бројеви[0];
нека други = бројеви[1];

// Резултат
први;
