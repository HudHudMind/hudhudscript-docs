// Ελληνικά - Λογική συνθηκών
έστω βαθμός = 85;

αν (βαθμός >= 90) {
    εκτύπωσε(1000);
} αλλιώς {
    αν (βαθμός >= 80) {
        εκτύπωσε(2000);
    } αλλιώς {
        εκτύπωσε(3000);
    }
}

εκτύπωσε(999);
