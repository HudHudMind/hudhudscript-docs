// فارسی - آرایه‌ها
// آرایه‌ها با کلمات کلیدی فارسی

// ساخت آرایه‌ها
بگذار اعداد = [۱, ۲, ۳, ۴, ۵];
بگذار نام‌ها = ["فاطمه", "علی", "مریم"];

// دسترسی به عناصر
بگذار اول = اعداد[۰];
بگذار دوم = اعداد[۱];

// نتیجه
اول;
