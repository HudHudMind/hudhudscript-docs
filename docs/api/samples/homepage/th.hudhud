// ไทย - ฟังก์ชันทักทาย

ฟังก์ชัน ทักทาย(ชื่อ) {
    ตัวแปร ข้อความ = "สวัสดี, " + ชื่อ + "!";
    พิมพ์(ข้อความ);
    return ข้อความ;
}

ฟังก์ชัน บวก(ก, ข) {
    return ก + ข;
}

ตัวแปร ผลลัพธ์ = ทักทาย("โลก");
พิมพ์(ผลลัพธ์);

ตัวแปร รวม = บวก(42, 58);
พิมพ์("รวม: " + รวม);
